#include <iostream>
using namespace std;

int main() {
  int n,l,sum;
  cout<<"enter the number";
  cin>>n;
  sum=0;
  while(n>0){
      l=n%10;
      sum=sum*10+l;
      n=n/10;
  }
  cout<<"reverse number is:"<<sum<<endl;
  return 0;
}
