#include <iostream>
using namespace std;

int main() {
    float n, x;
    int i;

    cout << "Enter number: ";
    cin >> n;

    x = n;

    for (i = 0; i < 10; i++) {
        x = 0.5 * (x + n / x);
    }

    cout << "Square root = " << x << endl;

    return 0;
}
